<h1>My Blog<h1>

<% @blog_posts.each do |blog_post| %>
<div>
  <div><%= link_to blog_post.title, blog_post_path(blog_post.id),class: "hover:text-blue-500" %></div>
  <p class="text-justify"><%= sanitize(blog_post.content).truncate(200, omission: "...")%></p>
</div>
<% end %>