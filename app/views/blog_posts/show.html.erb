<%= link_to "Edit", edit_blog_post_path(@blog_post), class: "hover:text-blue-500" if user_signed_in? %>

<div class="mt-4">
  <h1 class="mb-3"><%= @blog_post.title %></h1>
  <div class="mt-2 text-justify">
    <%= @blog_post.content %>
  </div>
</div>

